/*const cookieLogado = (req, res, next){
    if(req.cookie.email !== undefined && req.session.usuario == null){
//método que verifica a sessão para salvar o cookie
    }

}

//coockies estão definidos no login porém não está sendo exibido no navegador na ferramenta dos desenvolvedores


module.exports = cookieLogado;*/
